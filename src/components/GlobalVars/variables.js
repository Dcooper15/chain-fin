

// export default callIndex = ( {option },type, fixed) => { Object.keys(option.callExpDateMap).map((entry) => {
//     return Object.keys(option.callExpDateMap[entry]).map(
//       (innerArrayID) =>
//         option.callExpDateMap[entry][innerArrayID][0][type].toFixed(fixed)
//     );
//   })[0][1];
  
//}